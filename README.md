# Vuttara Website

Public site for Vuttara at `vuttara.nuttabuttainaa.com`.

## Deploy

```powershell
npm install
npm run deploy
```

After the first deployment, add `vuttara.nuttabuttainaa.com` as the project's custom domain in Cloudflare Pages.
